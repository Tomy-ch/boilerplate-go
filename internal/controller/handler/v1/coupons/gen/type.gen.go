// Package gen provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package gen

import (
	"time"

	openapi_types "github.com/oapi-codegen/runtime/types"
)

// Defines values for CouponDiscountKind.
const (
	CouponDiscountKindFlat CouponDiscountKind = "flat"
	CouponDiscountKindRate CouponDiscountKind = "rate"
)

// Valid indicates whether the value is a known member of the CouponDiscountKind enum.
func (e CouponDiscountKind) Valid() bool {
	switch e {
	case CouponDiscountKindFlat:
		return true
	case CouponDiscountKindRate:
		return true
	default:
		return false
	}
}

// Defines values for CouponDiscountInputKind.
const (
	CouponDiscountInputKindFlat CouponDiscountInputKind = "flat"
	CouponDiscountInputKindRate CouponDiscountInputKind = "rate"
)

// Valid indicates whether the value is a known member of the CouponDiscountInputKind enum.
func (e CouponDiscountInputKind) Valid() bool {
	switch e {
	case CouponDiscountInputKindFlat:
		return true
	case CouponDiscountInputKindRate:
		return true
	default:
		return false
	}
}

// Defines values for CouponScopeKind.
const (
	CouponScopeKindAll      CouponScopeKind = "all"
	CouponScopeKindCategory CouponScopeKind = "category"
	CouponScopeKindProduct  CouponScopeKind = "product"
)

// Valid indicates whether the value is a known member of the CouponScopeKind enum.
func (e CouponScopeKind) Valid() bool {
	switch e {
	case CouponScopeKindAll:
		return true
	case CouponScopeKindCategory:
		return true
	case CouponScopeKindProduct:
		return true
	default:
		return false
	}
}

// Defines values for CouponScopeInputKind.
const (
	CouponScopeInputKindAll      CouponScopeInputKind = "all"
	CouponScopeInputKindCategory CouponScopeInputKind = "category"
	CouponScopeInputKindProduct  CouponScopeInputKind = "product"
)

// Valid indicates whether the value is a known member of the CouponScopeInputKind enum.
func (e CouponScopeInputKind) Valid() bool {
	switch e {
	case CouponScopeInputKindAll:
		return true
	case CouponScopeInputKindCategory:
		return true
	case CouponScopeInputKindProduct:
		return true
	default:
		return false
	}
}

// CouponDiscount クーポンの値引き。いくら引くかを表します。適用範囲（どの明細が対象か）とは独立した軸です。
type CouponDiscount struct {
	// Kind 値引きの決まり方。`flat` は金額を直接差し引く定額、`rate` は対象額に率を掛ける定率です。
	//
	//
	// Example: rate
	Kind CouponDiscountKind `json:"kind"`

	// Value 種別における値。定額なら差し引く金額、定率なら対象額に掛ける率を、
	// 正確な十進量を保つ decimal 文字列で表します（例 `"0.10"` は 10% 引き）。
	// JSON number は IEEE754 double として復元され精度を失うため、文字列で表現します。
	//
	//
	// Example: 0.10
	Value string `json:"value"`
}

// CouponDiscountKind 値引きの決まり方。`flat` は金額を直接差し引く定額、`rate` は対象額に率を掛ける定率です。
//
// Example: rate
type CouponDiscountKind string

// CouponDiscountInput 発行するクーポンの値引き。いくら引くかを表します。適用範囲（どの明細が対象か）とは独立した軸です。
// 応答側の CouponDiscount と同じ形ですが、要求として受け取る側は宣言に無いフィールドを拒むため別に持ちます。
type CouponDiscountInput struct {
	// Kind 値引きの決まり方。`flat` は金額を直接差し引く定額、`rate` は対象額に率を掛ける定率です。
	//
	//
	// Example: rate
	Kind CouponDiscountInputKind `json:"kind"`

	// Value 種別における値。定額なら差し引く金額、定率なら対象額に掛ける率を、 正確な十進量を保つ decimal 文字列で表します（例 `"0.15"` は 15% 引き）。 ドメインの検証に落ちる値（定率で 0 以下または 1 超など）は 422 を返します。 JSON number は IEEE754 double として復元され精度を失うため、文字列で表現します。
	//
	// Example: 0.15
	Value string `json:"value"`
}

// CouponDiscountInputKind 値引きの決まり方。`flat` は金額を直接差し引く定額、`rate` は対象額に率を掛ける定率です。
//
// Example: rate
type CouponDiscountInputKind string

// CouponResponse クーポン。値引きと適用範囲という直交する 2 つの軸を持ちます。
// 1 枚のクーポンは 1 つの値引きと 1 つの適用範囲を持ち、複数枚の併用も複数範囲の合成も表しません。
type CouponResponse struct {
	// Discount クーポンの値引き。いくら引くかを表します。適用範囲（どの明細が対象か）とは独立した軸です。
	Discount CouponDiscount `json:"discount"`

	// ExpiresAt 有効期限。この時刻ちょうどを含めて以降は失効として扱います。
	// 失効を一括更新する機構は持たず、判定のたびに現在時刻と突き合わせます。
	//
	//
	// Example: 2026-10-01T00:00:00Z
	ExpiresAt time.Time `json:"expiresAt"`

	// Id クーポンID
	//
	// Example: 0193a1c0-0001-7000-8000-000000000002
	Id openapi_types.UUID `json:"id"`

	// IssuedAt 発行日時
	//
	// Example: 2026-09-01T00:00:00Z
	IssuedAt time.Time `json:"issuedAt"`

	// Scope クーポンの適用範囲。どの明細が対象かを表します。値引き（いくら引くか）とは独立した軸です。
	Scope CouponScope `json:"scope"`

	// UsedAt 使用日時。未使用の場合は null です。使用済みへの遷移は一度きりで取り消せません。
	UsedAt *time.Time `json:"usedAt"`
}

// CouponScope クーポンの適用範囲。どの明細が対象かを表します。値引き（いくら引くか）とは独立した軸です。
type CouponScope struct {
	// Kind 適用範囲の決まり方。`all` は対象を絞らない全体、`category` は特定の商品カテゴリに属する明細だけ、
	// `product` は特定の商品の明細だけを対象とします。
	//
	//
	// Example: category
	Kind CouponScopeKind `json:"kind"`

	// TargetId 範囲を絞る対象の識別子。`category` なら商品カテゴリID、`product` なら商品IDです。
	// `all` では対象を持たないため null になります。
	//
	//
	// Example: 5dd52d84-78eb-4a52-ba0b-2e11c95c2af2
	TargetId *openapi_types.UUID `json:"targetId"`
}

// CouponScopeKind 適用範囲の決まり方。`all` は対象を絞らない全体、`category` は特定の商品カテゴリに属する明細だけ、
// `product` は特定の商品の明細だけを対象とします。
//
// Example: category
type CouponScopeKind string

// CouponScopeInput 発行するクーポンの適用範囲。どの明細が対象かを表します。値引き（いくら引くか）とは独立した軸です。
// 応答側の CouponScope と同じ形ですが、要求として受け取る側は宣言に無いフィールドを拒むため別に持ちます。
type CouponScopeInput struct {
	// Kind 適用範囲の決まり方。`all` は対象を絞らない全体、`category` は特定の商品カテゴリに属する明細だけ、
	// `product` は特定の商品の明細だけを対象とします。
	//
	//
	// Example: category
	Kind CouponScopeInputKind `json:"kind"`

	// TargetId 範囲を絞る対象の識別子。`category` なら商品カテゴリ ID、`product` なら商品 ID です。 `all` では対象を持たないため null を送ります。 `category` / `product` で存在しない識別子を指した場合は 404 を返します。誰にも使えないクーポンを 発行してから気づくことになるためです。
	//
	// Example: 5dd52d84-78eb-4a52-ba0b-2e11c95c2af2
	TargetId *openapi_types.UUID `json:"targetId"`
}

// CouponScopeInputKind 適用範囲の決まり方。`all` は対象を絞らない全体、`category` は特定の商品カテゴリに属する明細だけ、
// `product` は特定の商品の明細だけを対象とします。
//
// Example: category
type CouponScopeInputKind string

// CouponsPostRequest クーポン発行のリクエスト（application/json）。admin のみ実行できます（非 admin は 403）。
// 受給者・値引き・適用範囲・有効期限を指定すると、その利用者へクーポンが 1 枚発行されます。
// **受給者は 1 人だけです。** 不特定多数への配布は別の操作（POST /v1/coupons/bulk-issue）です。
type CouponsPostRequest struct {
	// Discount 発行するクーポンの値引き。いくら引くかを表します。適用範囲（どの明細が対象か）とは独立した軸です。
	// 応答側の CouponDiscount と同じ形ですが、要求として受け取る側は宣言に無いフィールドを拒むため別に持ちます。
	Discount CouponDiscountInput `json:"discount"`

	// ExpiresAt 発行するクーポンの有効期限。締切を名指しする操作なので、日数ではなく絶対時刻で受け取ります。 現在時刻以前を指定した場合は、発行した瞬間に失効するクーポンになるため 422 を返します。
	//
	// Example: 2026-12-31T14:59:59Z
	ExpiresAt time.Time `json:"expiresAt"`

	// Scope 発行するクーポンの適用範囲。どの明細が対象かを表します。値引き（いくら引くか）とは独立した軸です。
	// 応答側の CouponScope と同じ形ですが、要求として受け取る側は宣言に無いフィールドを拒むため別に持ちます。
	Scope CouponScopeInput `json:"scope"`

	// UserId 受給者の利用者 ID。退会していない利用者に限ります。存在しない、または退会済みの ID を 指定した場合は 404 を返します。受給者は発行時に確定し、以後移りません。
	//
	// Example: 0193a1c0-0000-7000-8000-000000000001
	UserId openapi_types.UUID `json:"userId"`
}

// ErrorResponse エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type ErrorResponse struct {
	// Code 機械的に処理可能なエラーコード
	//
	// Example: EXAMPLE_ERROR
	Code string `json:"code"`

	// Message 人間が読めるエラーメッセージ
	//
	// Example: 定義されたエラーメッセージ
	Message string `json:"message"`

	// RequestId リクエストID
	//
	// Example: abcdefghijklmnopqrstuvwxyz
	RequestId string `json:"requestId"`
}

// ErrorResponseWithDetails 詳細識別子付きエラーレスポンススキーマ。 この schema を error レスポンスに宣言したエンドポイントだけが details を返せる（opt-in）。 未宣言のエンドポイントでは errorhandler が details を fail-closed で落とす。
type ErrorResponseWithDetails struct {
	// Code 機械的に処理可能なエラーコード
	//
	// Example: EXAMPLE_ERROR
	Code string `json:"code"`

	// Details エラー詳細を示す公開可能な識別子(任意)。検証エラーでは不正なフィールド名が入る
	//
	// Example: ["firstName","email"]
	Details *[]string `json:"details,omitempty"`

	// Message 人間が読めるエラーメッセージ
	//
	// Example: 定義されたエラーメッセージ
	Message string `json:"message"`

	// RequestId リクエストID
	//
	// Example: abcdefghijklmnopqrstuvwxyz
	RequestId string `json:"requestId"`
}

// IdempotencyKeyParam Example: 3fa85f64-5717-4562-b3fc-2c963f66afa6
type IdempotencyKeyParam = string

// BadRequest400 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type BadRequest400 = ErrorResponse

// Conflict409 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Conflict409 = ErrorResponse

// Forbidden403 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Forbidden403 = ErrorResponse

// InternalServerError500 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type InternalServerError500 = ErrorResponse

// MethodNotAllowed405 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type MethodNotAllowed405 = ErrorResponse

// NotFound404 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type NotFound404 = ErrorResponse

// ServiceUnavailable503 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type ServiceUnavailable503 = ErrorResponse

// Unauthorized401 エラーレスポンスの共通スキーマ（base）。 details は返さない。details を返すエンドポイントは ErrorResponseWithDetails を参照する。
type Unauthorized401 = ErrorResponse

// UnprocessableEntity422 詳細識別子付きエラーレスポンススキーマ。 この schema を error レスポンスに宣言したエンドポイントだけが details を返せる（opt-in）。 未宣言のエンドポイントでは errorhandler が details を fail-closed で落とす。
type UnprocessableEntity422 = ErrorResponseWithDetails

// PostCouponsParams defines parameters for PostCoupons.
type PostCouponsParams struct {
	// IdempotencyKey 冪等キー。非冪等な変更操作（作成系など）で、同一キーによるリトライを重複実行なく
	// 安全にするための識別子。サーバは同一 (認証主体, キー) の再送を初回結果のリプレイとして扱う。
	// このヘッダを宣言する操作は、ハンドラが必ず idempotency.Run 経由で処理する契約（完全性テストで機械検証）。
	IdempotencyKey *IdempotencyKeyParam `json:"Idempotency-Key,omitempty"`
}

// PostCouponsJSONRequestBody defines body for PostCoupons for application/json ContentType.
type PostCouponsJSONRequestBody = CouponsPostRequest
